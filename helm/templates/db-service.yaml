apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.db.name }}
  labels:
    app: {{ .Values.db.name }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.db.name }}
  ports:
  - name: "db-service"
    port: 5432
    targetPort: 5432
